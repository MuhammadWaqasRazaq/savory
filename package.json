{
  "name": "savory",
  "version": "1.0.0",
  "private": true,
  "description": "Recipe Sharing Website - Savory",
  "scripts": {
    "build": "cd backend && npm ci && cd ../frontend && npm ci && npm run build",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": "20.x"
  }
}